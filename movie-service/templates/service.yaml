apiVersion: v1
kind: Service
metadata:
  name: movie_service
  namespace: dev 
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8001
  selector:
    app: {{ .Release.Name }}
